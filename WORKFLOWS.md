# MoeSR 工作流说明

## 使用前准备

1. 确认 models/ 中已经存在工作流引用的 ONNX 模型。模型目录默认被 Git 忽略，不会随仓库提交。
2. 启动 MoeSR，进入“工作流”页面。
3. 在工作流下拉框中选择工作流；首次加载新文件后，切换页面再返回即可刷新列表。
4. 选择输入图片或文件夹，选择输出目录，然后运行工作流。
5. 工作流中的 GPU ID 默认是 0。Tile 是每个推理块的边长，显存不足时按 384 -> 256 -> 128 降低；小图不要盲目使用过大的 Tile。

工作流节点按从上到下执行。scale 节点的分数是相对于当前图片尺寸的比例，例如 1/2 表示缩小一半。工作流使用的模型均为日系插画/ACGN 方向，不能把生成的细节当作原图真实信息。

## 工作流总览

| 文件 | 目标 | 实际输出 |
| --- | --- | --- |
| 小图实验性工作流.json | 空白实验模板 | 由用户自行添加节点 |
| 小图降噪保细节2x.json | 小图修复并保守放大 | 2x |
| 全分辨率自适应修复.json | 自动按分辨率选择策略 | 4x、2x 或 1x |
| 极致修复2x.json | 现有模型中计算量最高的合理叠加 | 2x |
| Fix2强降噪稳定2x.json | 强力清除压缩和伪影 | 2x |
| 大图保真修复原尺寸.json | 大图保真修复，不改变最终尺寸 | 1x |

## 1. 小图实验性工作流

文件：workflows/小图实验性工作流.json

这是一个空模板，只有输入和输出节点，没有推理节点，不能直接运行。适合复制后自行试验节点顺序、条件跳转和缩放比例。

## 2. 小图降噪保细节2x

文件：workflows/小图降噪保细节2x.json

处理链：

HATGAN x1 fix_only -> HATGAN x4 jp_Illustration-fix1 -> scale 1/2

- 第一阶段在原尺寸修复压缩和模糊，不主动放大。
- 第二阶段重建小图中的轮廓和局部细节。
- 最后缩小一半，压制过度锐化和不稳定的假纹理。
- 默认 Tile 为 256，适合低分辨率、轻到中度压缩的插画。
- 输入短边低于约 256px 时，可将两个推理节点的 Tile 都改为 128。

## 3. 全分辨率自适应修复

文件：workflows/全分辨率自适应修复.json

工作流先检查宽度和高度，使用“任一边”触发规则：

- 宽、高都小于 512px：HATGAN x1 -> HATGAN x4，Tile 128，输出 4x。
- 任一边小于 1024px：HATGAN x1 -> HATGAN x4，Tile 256，再 1/2，输出 2x。
- 宽、高都在 1024-2560px：ESRGAN fix1-d x4，Tile 256，再 1/4，保持 1x。
- 任一边大于 2560px：HATGAN x1 fix_only，Tile 384，保持 1x，避免超大的 x4 中间图。

这是混合尺寸文件夹的默认选择。它优先让小图获得重建，对常规大图使用保守修复，对超大图控制主机内存和耗时。

## 4. 极致修复2x

文件：workflows/极致修复2x.json

处理链：

HATGAN x1 fix_only -> HATGAN x4 jp_Illustration-fix1 -> scale 1/2 -> ESRGAN fix1-d x4 -> scale 1/4

- 先在原尺寸清理输入，避免放大模型把噪点当成纹理。
- HATGAN x4 主动重建小图细节。
- 第一次回缩将不稳定的细节收敛到 2x。
- ESRGAN fix1-d 进行保守精修，再回缩到最终 2x。

这是最慢、最吃主机内存的工作流，适合损坏明显的小型或中型插画。干净原图不建议使用；多个生成模型串联会增加风格偏移和伪细节风险。建议关闭“工作流中保持模型加载”，先处理单张样图。

## 5. Fix2 强降噪稳定2x

文件：workflows/Fix2强降噪稳定2x.json

处理链：

ESRGAN fix2 x4 -> scale 1/2 -> HATGAN x1 fix_only

- fix2 的修复力度强，优先处理重压缩、块状伪影、明显模糊和噪点。
- 回缩到 2x，减少 fix2 可能产生的过度锐化和伪纹理。
- HATGAN x1 在目标尺寸做最后稳定修复，不再放大。

适合压缩明显的小图和中图。对线稿、文字、网点、原本干净的高分辨率图像要谨慎，因为 fix2 可能主动改写细节。

## 6. 大图保真修复原尺寸

文件：workflows/大图保真修复原尺寸.json

处理链：

ESRGAN fix1-d x4 -> scale 1/4

- 利用 x4 模型的修复能力处理大图中的轻度模糊和 JPEG 痕迹。
- 通过 1/4 回到原始分辨率，最终不放大。
- Tile 为 384，在 V100 16GB 上优先减少切块数量；显存报错时降到 256。

适合已具有较多真实细节、但需要清理和保真的大图。它仍会先生成临时 4x 图，因此 4K 以上图片会占用较多主机内存；超大批处理优先使用自适应工作流的 HATGAN x1 档位。

## 模型和输出注意事项

- 当前仓库没有 MoeIR ONNX 权重；界面中的 MoeIR 入口不会列出可用模型。
- 当前保存逻辑固定编码为 PNG，不支持 WebP 无损输出。
- 工作流中的 scale 节点会改变实际图片尺寸，但当前默认文件名中的 {scale} 仍主要按推理模型倍率记录；看到文件名倍率与实际尺寸不一致时，以图片实际尺寸为准。
